- id: check-forbidden-filetypes
  name: Check for forbidden file types
  description: Block data files that may contain sensitive information
  entry: pre-commit-check/check-filetypes.py
  language: script
  types: [file]
  pass_filenames: true

- id: check-personal-info
  name: Check for personal information
  description: Detect Dutch names, addresses, and patient IDs
  entry: pre-commit-check/check-personal-info.py
  language: script
  types: [text]
  pass_filenames: true

- id: check-forbidden-filetypes-prepush
  name: Check forbidden file types (pre-push)
  entry: pre-push-check/check-filetypes-prepush.py
  language: script
  stages: [pre-push]
  always_run: true
  pass_filenames: false

- id: check-personal-info-prepush
  name: Check for personal information before push
  entry: pre-push-check/check-personal-info-prepush.py
  language: script
  stages: [pre-push]
  always_run: true
  pass_filenames: false

# Bash versions (legacy, for backwards compatibility)
- id: check-forbidden-filetypes-bash
  name: Check for forbidden file types (bash)
  description: Block data files that may contain sensitive information
  entry: pre-commit-check/check-filetypes.sh
  language: script
  types: [file]
  pass_filenames: true

- id: check-personal-info-bash
  name: Check for personal information (bash)
  description: Detect Dutch names, addresses, and patient IDs
  entry: pre-commit-check/check-personal-info.sh
  language: script
  types: [text]
  pass_filenames: true

- id: check-forbidden-filetypes-prepush-bash
  name: Check forbidden file types (pre-push, bash)
  entry: pre-push-check/check-filetypes-prepush.sh
  language: script
  stages: [pre-push]
  always_run: true
  pass_filenames: false

- id: check-personal-info-prepush-bash
  name: Check for personal information before push (bash)
  entry: pre-push-check/check-personal-info-prepush.sh
  language: script
  stages: [pre-push]
  always_run: true
  pass_filenames: false
